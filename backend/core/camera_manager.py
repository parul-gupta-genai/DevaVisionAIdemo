
class CameraManager:
    running_cameras = {}
    def start_command_listener(self, *a): pass
    def start_global_workers(self): pass
    def start_all_active_cameras(self): pass
    def stop_camera_pipeline(self, *a): pass
    def stop_global_workers(self): pass
    def evaluate_auto_suspend(self, *a): pass
camera_manager = CameraManager()
